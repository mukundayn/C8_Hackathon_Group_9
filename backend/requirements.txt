# Netra.ai / Incident Suite — backend dependencies
# Install (inside a venv):  pip install -r requirements.txt
#
# Python: 3.11–3.13 recommended (Render: set PYTHON_VERSION=3.12).
# Production (Render free tier): set HF_TOKEN so embeddings use the
# HuggingFace Inference API instead of loading local sentence-transformers/torch
# into a 512MB instance.

# ---- LangGraph orchestration + LLM (OpenRouter) ----
langgraph>=0.2.0,<2.0.0
langchain>=0.3.0,<2.0.0
langchain-core>=0.3.0,<2.0.0
langchain-openai>=0.2.0,<2.0.0

# ---- RAG: Chroma + embeddings + hybrid search ----
langchain-chroma>=0.1.4,<2.0.0
langchain-huggingface>=0.1.0,<2.0.0
sentence-transformers>=3.0.0,<6.0.0
rank_bm25>=0.2.2,<1.0.0

# ---- Web / API ----
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0
sse-starlette>=2.1.0,<4.0.0
python-multipart>=0.0.9,<1.0.0
httpx>=0.27.0,<1.0.0

# ---- Config / models ----
python-dotenv>=1.0.0,<2.0.0
pydantic>=2.0.0,<3.0.0

# ---- HuggingFace Hub (fallback knowledge + optional datasets) ----
datasets>=2.14.0,<6.0.0
huggingface-hub>=0.20.0,<2.0.0

# ---- Optional: LangSmith eval tracking ----
langsmith>=0.1.0,<1.0.0

# ---- Optional: MCP server tooling ----
mcp>=1.0.0,<2.0.0
